# 项目分析: Flai Agent (深壤 Agent)

## 项目概览
Flai Agent 是一个基于 FastAPI 构建的 AI 对话代理服务。它的设计目标是提供灵活的配置和强大的记忆功能。

### 核心功能
*   **多角色支持**: 通过 `config/prompts/characters.json` 配置多个角色。
*   **记忆系统**:
    *   **短期记忆**:存储在 MySQL (`t_dialogue`) 中，保留最近 7 轮对话。
    *   **持久化记忆**: 存储在 MySQL (`chat_memory`) 中，包括 LLM 提取的用户画像（长期和短期）。
    *   **向量记忆**: (可选) 使用 Qdrant 进行语义检索，以补充上下文。
*   **情绪系统**: 能够识别用户情绪并调整 AI 的回复（支持多种情绪状态）。
*   **语音合成 (TTS)**: 集成语音服务，支持将文字转换为语音并上传到 OSS。
*   **热更新**: 支持在不重启服务的情况下更新 Prompt 和配置。
*   **日志系统**: 完善的日志记录，按日期归档。

### 优化任务 (进行中)
*   [x] **后台任务化**: 将 `memory.save_conversation` 移入 `FastAPI BackgroundTasks`，减少用户等待时间。
*   [x] **优化超时体验**: `LLMService` 增加超时设置 (15s) 和减少重试次数 (5->3)，避免长时间无响应。
*   [x] **单元测试**: 增加 `unit_test` 文件夹，并添加相关测试用例。
*   [x] **对话历史组装优化**: 将历史对话作为多个连续的用户/助手消息发送给LLM，而非嵌入到单个用户消息中
*   [x] **昵称偏好处理**: 增加对用户昵称偏好的识别与处理机制，确保正确理解用户角色关系
*   [x] **记忆上下文整合**: 将长短记忆描述直接整合到system prompt中，而非作为独立的system消息添加
*   [x] **Prompt模板优化**: 移除过时占位符，简化用户提示词模板
*   [x] **对话历史时间顺序修复**: 修正对话历史处理逻辑，确保按正确时间顺序排列，使LLM能正确理解上下文
*   [x] **语音参数传递修复**: 修复第三方角色对话历史查询中语音参数未正确传递的问题
*   [x] **用户消息与系统指令分离**: 将JSON格式指令从用户消息中分离，作为独立的系统消息发送，使对话更清晰
*   [x] **口癖词随机删除**: 实现50%概率删除分割后第一个句子中的单独口癖词（如"哼"、"哈"、"哎呀"等），提高对话自然度
*   [x] **Emoji回复策略**: 当用户输入全是emoji时，AI以90%概率也用emoji回复
*   [x] **userId别名支持**: 为ChatRequest模型添加userId别名支持，提高API兼容性

*   [x] **变量错误修复**: 修复向量存储服务中 `filtered_user_message` 变量未定义的错误，在异常处理中添加特定的 NameError 捕获

### 代码整理与维护 (2026-02-01)
*   [x] **清理一次性工具脚本**: 删除已完成任务的角色导入脚本（add_missing_batch.py, add_missing_characters.py, add_missing_openers_batch.py, test_character_lookup.py, test_fixes.py），这些脚本包含硬编码路径，不再需要保留
*   [x] **移除 lingma 配置**: 删除 `.lingma/` 目录和 `.lingmaignore` 文件，并添加到 `.gitignore` 防止再次提交
*   [x] **分支合并同步**: 将 company_pc 分支的所有改动合并到 online 和 main 分支，解决合并冲突（删除旧的 character_other_info.py，采用 JSON 配置方案）

### 副本世界系统开发 (2026-02-01)
*   [x] **数据库表设计**: 创建副本世界相关数据表
    *   `t_instance_world_session` - 会话/存档表
    *   `t_instance_world_dialogue` - 对话历史表
    *   `t_prompt_config` - Prompt配置统一表 (GM/人物/世界)
*   [x] **Prompt配置数据库化**: 将 GM、第三方人物、世界配置统一存入数据库
    *   创建 SQLAlchemy 模型 `PromptConfig`
    *   创建数据迁移脚本，导入 104 条配置 (8 GM + 88 人物 + 8 世界)
    *   更新 `instance_world_prompts.py` 优先从数据库加载，文件作为 fallback
    *   人物 ID 保持原始格式 (如 c1s1c1_0001)，不加前缀
*   [x] **建表语句归档**: 保存建表 SQL 到 `unit_test/create_instance_world_tables.sql`，便于生产环境建表

## 下一步计划 (建议)
*   验证 Qdrant 集成（如果需要向量记忆）。
*   扩展 `config/prompts/` 中的角色配置。
*   测试并微调情绪分析的准确性。
*   完成副本世界 SSE 接口联调测试。
*   生产环境部署副本世界相关数据表。